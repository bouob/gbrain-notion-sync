{
  "name": "notion-sync",
  "version": "0.1.3",
  "description": "Sync Notion PAI databases to gbrain knowledge graph",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-pull.mjs",
    "sync:dry": "node scripts/sync-pull.mjs --dry-run",
    "postprocess": "node scripts/postprocess.mjs",
    "doctor": "node scripts/doctor.mjs",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
